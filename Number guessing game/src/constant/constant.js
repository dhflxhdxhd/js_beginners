const HINT_MESSAGE = Object.freeze({
    HIGH : "Number is high",
    LOW : "Number is low",
});

const ERROR = Object.freeze({
    INPUT_RANGE : "1이상 100이하의 수를 입력하세요.",
});

const RESULT = Object.freeze({
    SUCCESS : "Success",
    FAIL : "Game Over",
});

const SENTENCE = Object.freeze({
    PREVIOUS_GUESS : "previous Guess : ",
})